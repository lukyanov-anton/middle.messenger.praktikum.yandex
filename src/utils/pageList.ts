/* import { Block } from "../core";

export enum Pages {
  Login = "login",
  Signin = "signin",
  Profile = "profile",
}

const map: Record<Pages, typeof Block> = {
  [Pages.Login]: LoginPage,
};

export const getPageComponent = (page: Pages): typeof Block => {
  return map[page];
};
 */
